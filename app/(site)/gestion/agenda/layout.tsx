export const metadata = {
  title: "Gestion agenda",
  robots: {
    index: false,
    follow: false
  }
};

export default function AgendaAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
